package org.example;

import java.util.*;

public class CollectionsDrillsProblems {

    // 1. Reverse a List in-place
    public static <T> void reverseList(List<T> list) {
        Collections.reverse(list);
    }

    // 2. Rotate a Deque to the right by k steps
    public static <T> void rotateDequeRight(Deque<T> deque, int k) {
        // TODO: Rotate deque to the right k times

    }

    // 3. Compute prefix sums of a List of Integers
    public static List<Integer> prefixSums(List<Integer> list) {
        // TODO: Return a new list containing prefix sums
        return null;
    }

    // 4. Find if any pair in a sorted List sums to the target (Two-pointer pattern)
    public static boolean hasPairWithSum(List<Integer> sortedList, int target) {
        // TODO: Use two pointers to find if a pair adds to target
        return false;
    }

    // 5. Frequency count of values in a List
    public static <T> Map<T, Integer> frequencyCount(List<T> list) {
        // TODO: Return a map of value frequencies
        return null;
    }
}
